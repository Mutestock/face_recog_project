import cli

# To be deleted. 
# Use pip install --editable .in project root instead.
# Then run frecog (insert cli command).
# This ensures that the user has all the necessary dependencies.
# Setup.py must be populated every time a new library is used.

if __name__ == "__main__":
    cli.frecog()